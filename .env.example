# Proveedor de IA: ollama (default) | mock
VITE_IA_PROVIDER=ollama

# Modelo preferido en Ollama (si no esta, usa gemma3:* o el primero disponible)
VITE_OLLAMA_MODEL=gemma4

# Base del proxy Vite -> Ollama
# VITE_OLLAMA_BASE=/api/ollama
